// Locale type kept for backward compatibility with any external references.
// All translation data is now in messages/en.json and messages/pt.json.
// Language detection and storage is handled by i18next (lib/i18n.ts).
export type Locale = "en" | "pt";
